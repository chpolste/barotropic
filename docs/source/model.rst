BarotropicModel
===============

A :py:class:`.BarotropicModel` is a representation of the barotropic PV equation and provides temporal integrators to advance the flow contained in :py:class:`.State` objects in time.
The forcing and the numerical diffusion are configured with the :py:class:`.BarotropicModel`.


.. autoclass:: barotropic.BarotropicModel

